[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "ml-from-scratch"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
python-source = "src"
bindings = "ml_from_scratch.rust_core"
